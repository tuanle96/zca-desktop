# Rust / Tauri
/target
/apps/desktop/src-tauri/target
/apps/server/target
/crates/zca-types/target
**/*.rs.bk

# Node / frontend
node_modules
dist
dist-ssr
.vite
.svelte-kit
/apps/desktop/build

# Zalo credential exports — NEVER commit (cookies are bearer tokens)
*.cred.json
.zalo-cred.json
cookies.json
/credentials/

# Local env files with secrets (docker compose reads these) — keep *.example templates
.env
.env.*
!.env.*.example

# Local Cloudflare tunnel configs/credentials
/cloudflared/zca-dev/config.local.yml

# Local debug logs + raw API captures (may contain redacted/raw payloads)
/logs/
*.log

# Internal agent / dev tooling — not part of the published project
/.harness/
/.agents/
/.kiro/
/.codex/
/.code-review-graph/
.agent-worktrees/
/.claude/
/plans/
/reports/

# One-time OSS-prep helper (not for publishing)
/oss-prepare-public.sh

# OS / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
